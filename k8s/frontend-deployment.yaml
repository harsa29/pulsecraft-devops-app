apiVersion: apps/v1
kind: Deployment
metadata:
  name: pulsecraft-frontend
  namespace: pulsecraft
  labels:
    app: pulsecraft-frontend
    tier: web
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: pulsecraft-frontend
  template:
    metadata:
      labels:
        app: pulsecraft-frontend
        tier: web
    spec:
      containers:
        - name: frontend
          image: pulsecraft/frontend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              name: http
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
