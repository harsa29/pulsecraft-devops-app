apiVersion: v1
kind: Service
metadata:
  name: pulsecraft-backend-service
  namespace: pulsecraft
  labels:
    app: pulsecraft-backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: pulsecraft-backend
