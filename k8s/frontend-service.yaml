apiVersion: v1
kind: Service
metadata:
  name: pulsecraft-frontend-service
  namespace: pulsecraft
  labels:
    app: pulsecraft-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: pulsecraft-frontend
